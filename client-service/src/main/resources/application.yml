spring:
  datasource:
    url: jdbc:postgresql://db_client_service:5432/client_db
    username: client
    password: client
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
